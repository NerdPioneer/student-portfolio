Cleanup: trim dead SEO metas, drop stale source files, add HTML minify step

- index.html: strip ~50 non-standard/ignored meta tags (keywords, search-terms,
  career-level, geo placeholders, duplicate author/title, etc.); keep only
  legitimate metadata (title, description, robots, OG, Twitter, canonical,
  geo.region, profile:*) and consolidate msapplication-TileColor to brand color
- index.html: remove unnecessary onerror fallback handlers from carousel images
- Delete unused src/styles.css (legacy 71 KB file, not referenced anywhere)
- Delete unused src/components/hero.html and project-card.html (scaffolding,
  never included); drop matching glob from tailwind.config.js
- Add html-minifier-terser-based build:html step (scripts/build-html.mjs)
  emitting dist/index.html; wire into npm run build (~36% size reduction)
